# Contributing

欢迎提交问题、文档改进和可复现的新模块。请让每次变更保持范围清晰、来源可追踪，并遵守以下约定。

1. 不提交密钥、账户标识、许可证信息、私人文库快照或受限数据。
2. 新增数据或全文前先确认再分发许可；不能确认时只提交来源、下载说明和哈希。
3. 修改 schema 时说明兼容性，并同步更新示例与消费者。
4. 实证功能应包含最小可验证测试；报告实际支持的模型边界。
5. 文献条目必须核验真实存在；涉及方法、结论或页码的摘要必须基于已读全文。
6. 结果不理想、全文不可得或某个 agent 未完成时，应明确记录，不得静默删除。

提交拉取请求前，请运行相关测试并检查 `git diff --cached`。若贡献包含新的第三方材料，请在请求中说明来源与许可。
